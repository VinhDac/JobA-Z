# One board per company. Every line was verified to return real postings (2026-09-04).
# To add a company: open their careers page, take the slug from the URL, rerun the scan.
#
# Removed: marshallwace, optiver — the board returns 200 but is EMPTY; they post elsewhere.

[greenhouse]
boards = [
  # funds / quant — the band being aimed at
  "mangroup", "janestreet", "imc", "point72", "aqr",
  "squarepointcapital", "gsacapital", "winton", "quadraturecapital",
  # fintech UK
  "monzo", "wise", "tide", "ebury", "gocardless",
]

[lever]
boards = ["zopa", "spotify", "palantir"]

[ashby]
boards = ["ramp", "cohere", "elevenlabs", "synthesia"]
